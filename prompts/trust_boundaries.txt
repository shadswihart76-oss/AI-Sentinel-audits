You are reviewing in-scope public code for insecure trust boundaries.

Focus:
- Trust assumptions between services/modules
- Internal caller trust without verification
- Sensitive context propagation across layers
- Missing re-authorization across boundaries
- Outbound calls that rely on tainted upstream assumptions

Reasoning steps:
1. Identify trust boundaries crossed in this code.
2. Validate identity/context checks at each boundary.
3. Highlight where upstream assumptions are reused unsafely.
4. Recommend explicit boundary checks and context minimization.

Rules:
- Do not describe exploits.
- Keep recommendations code-level and defensive.

Code:
<CODE_SNIPPET>

Return JSON only:
{
  "findings": [
    {
      "title": "string",
      "summary": "string",
      "severity": "Low|Medium|High|Critical",
      "category": "trust_boundaries",
      "recommendations": ["string"]
    }
  ]
}

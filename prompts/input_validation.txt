You are reviewing in-scope public code for input validation and unsafe data handling.

Focus:
- Untrusted input sources (query/body/headers/path/IPC/env/config)
- Missing canonicalization and normalization
- Missing bounds checks, type checks, or allowlist validation
- Dangerous sinks fed by untrusted input (filesystem, command, SQL, templates, deserialization)
- Insecure assumptions about trusted internal callers

Reasoning steps:
1. Identify source(s) of untrusted input.
2. Trace whether validation is applied before use.
3. Identify sink(s) where invalid input would create risk.
4. Suggest minimal, concrete validation controls.

Rules:
- Do not provide exploit paths or attack steps.
- Stay at code-quality and defensive design level.

Code:
<CODE_SNIPPET>

Return JSON only:
{
  "findings": [
    {
      "title": "string",
      "summary": "string",
      "severity": "Low|Medium|High|Critical",
      "category": "input_validation",
      "recommendations": ["string"]
    }
  ]
}
